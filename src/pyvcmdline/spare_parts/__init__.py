"""
ce module python va contenir les "spare parts" c-à-d
 pièces détachées que la commande pyv-cli utilise pour assembler un
 bundle spécifique, à la demande.

 On utilise aussi bien des cartouches de jeu qui servent de template,
 que le fichier spécial launch_game.py
 décliné en version bundle isolé, ou version bundle réseau...

 Il faut aussi être capable de générer le connecteur py,
 lorsqu'on fait de l'assemblage de bundle réseau.
 Le script autogen qui s'occupe de ça se base sur API_SPEC.json
"""
